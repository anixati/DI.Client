import { createStyles } from '@mantine/core';

export const FieldStyles = createStyles((theme) => ({
  FieldGroup: {
    paddingBottom: 15,
    borderBottom: `1px solid  ${theme.colors['gray'][1]}`,
  },
}));

import { Container } from '@mantine/core';

export interface SimpleCmdProps {
  title: string;
}

export const SimpleCmd: React.FC<SimpleCmdProps> = (rx) => {
  return (
    <Container>
      <p>...</p>
    </Container>
  );
};
